name: linksentry
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  # Existing dependencies
  tldts: ^0.0.1-beta
  url_launcher: ^6.3.0
  fl_chart: ^0.69.0 
  mobile_scanner: ^5.0.0
  google_mlkit_text_recognition: ^0.13.0
  image_picker: ^1.0.7
  cupertino_icons: ^1.0.8
  firebase_core: ^4.4.0 
  cloud_firestore: ^6.1.2
  firebase_auth: ^6.1.4
  google_sign_in: ^6.3.0
  http: ^1.6.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2

  # Additional dependencies for the threat engine
  path: ^1.9.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/ 
    - assets/models/ 